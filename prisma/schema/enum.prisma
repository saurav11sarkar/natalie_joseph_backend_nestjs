enum UserRole {
  admin
  user
}

enum Status {
  pending
  approved
  rejected
}

enum PaymentStatus {
  pending
  completed
  failed
  refunded
}

enum CreditDirection {
  credit
  debit
}

enum CreditReason {
  purchase
  opening_balance
  subscription_grant
  subscription_expiry
  expiry
  photo
  voice
  extra_message
  gift
}

enum ChatMessageType {
  text
  voice
  photo
  gift
}

enum GiftTransactionStatus {
  completed
  refunded
}
